<% if object.errors.any? %>
  <div class="ui form error">
    <div class="ui error message">
      O formulário <%= "contém".pluralize(object.errors.count) %> <%= object.errors.count %> <%= "erro".pluralize(object.errors.count) %>.
    
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
    </div>
    <br>
  </div>
<% end %>